# Connection properties for host pg.example.com
# No secrets here: password_env names an environment variable instead.
port=5432
database=sales
username=sales_reader
password_env=PG_SALES_PASSWORD

# Optional SQLAlchemy driver options (any "driver.<name>" entry is passed through)
driver.sslmode=require

# Optional Purview overrides for this source
purview_source_host=pg.example.com
purview_database=sales
